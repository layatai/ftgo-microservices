{
  "name": "ftgo-seed-scripts",
  "version": "1.0.0",
  "description": "Seed scripts for FTGO microservices",
  "private": true,
  "scripts": {
    "seed": "node seed-data.js",
    "seed:menus": "node seed-data.js --menus-only",
    "seed:orders": "node seed-data.js --orders-only"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}

